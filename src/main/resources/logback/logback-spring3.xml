<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 로그파일 저장 경로 -->
    <property name="LOG_DIR" value="./logs" />
<!--    <property name="LOG_PATTERN" value="./logs/application-%d{yyyy-MM-dd-HH-mm}.%i.log" />-->

    <!--     CONSOLE 저장-->
    <appender class="ch.qos.logback.core.ConsoleAppender" name="CONSOLE">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %highlight([%-5level]) %cyan(%d{yyyy-MM-dd HH:mm:ss.SSS}) : [%thread] %green([%logger:%line]) - %msg%n
            </Pattern>
        </layout>
    </appender>
<!--    &lt;!&ndash; LOGSTASH &ndash;&gt;-->
    <appender class="net.logstash.logback.appender.LogstashTcpSocketAppender" name="logstash">
        <destination>localhost:5000</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>
    <!--     FILE 저장-->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
<!--        <file>${LOG_DIR}/application.log</file>-->
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %highlight([%-5level]) %cyan(%d{yyyy-MM-dd HH:mm:ss.SSS}) : %green([%logger:%line]) - %msg%n
            </Pattern>
        </layout>
<!--        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/application_%d{yyyy-MM-dd}_%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
    </appender>
    <logger level="INFO" name="org.hibernate.SQL"/>
    <root level="INFO">
        <!--    <appender-ref ref="FILE" />-->
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
<!--        <appender-ref ref="logstash"/>-->
    </root>
</configuration>
